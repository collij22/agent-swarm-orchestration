{
    "comment": "NEVER commit real secrets. Use environment variables or secure secret management.",
    "development": {
        "github": {
            "token": "dev_github_token_placeholder",
            "webhook_secret": "dev_webhook_secret_placeholder"
        },
        "openai": {
            "api_key": "dev_openai_key_placeholder"
        },
        "google_oauth": {
            "client_id": "dev_client_id_placeholder",
            "client_secret": "dev_client_secret_placeholder"
        }
    },
    "production": {
        "github": {
            "token": "$GITHUB_TOKEN",
            "webhook_secret": "$GITHUB_WEBHOOK_SECRET"
        },
        "openai": {
            "api_key": "$OPENAI_API_KEY"
        },
        "google_oauth": {
            "client_id": "$GOOGLE_OAUTH_CLIENT_ID",
            "client_secret": "$GOOGLE_OAUTH_CLIENT_SECRET"
        }
    },
    "security_guidelines": [
        "Use environment variables for sensitive data",
        "Rotate secrets regularly",
        "Never hardcode secrets in source files",
        "Use a secrets management service in production",
        "Limit secret access to necessary services"
    ]
}